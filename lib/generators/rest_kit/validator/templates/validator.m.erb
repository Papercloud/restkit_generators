// RKGValidator.m
// Generated by Restkit Generators - <%= Time.now.strftime('%Y/%m/%d') %>
//
// Wrapper class for your chosen validator to work with the generated +Validation categories.
// Implement the `addPresenceValidatorForField:` and `hasErrors`.

#import "<%= validator_filename %>.h"

@interface RKGValidator ()
@property (nonatomic, strong) id object;
//@property (nonatomic, strong) MyValidator * validator;
@end

@implementation RKGValidator

- (id)initWithObject:(id)object;
{
    if(self = [super init]) {
        self.object = object;
        // self.validator = [[MyValidator alloc] initWithObject:self.object];
        return self;
    }

    return nil;
}

- (void)addPresenceValidatorForField:(NSString *)field
{
    // Use your choice of validator to add presence validators.
    // [self.validator addValidationForAttribute:field];
}

- (BOOL)hasErrors
{
    // Implement your validation here.
    // self.invalidKeyPaths = [self.validator validate];

    // if (self.invalidKeyPaths.count > 0) {
    //     return true;
    // }

    return false;
}

@end