#import "<%= filename %>.h"

@implementation <%= filename %>

@dynamic <%= columns.map{|c| ios_attr_name(c.name) }.join(", ") %>;
@dynamic <%= associations.map{|a| ios_attr_name(a.name) }.join(", ") %>;
@dynamic <%= has_many_associations.map{|a| ios_attr_name(a.name.to_s.singularize + "_ids") }.join(", ") %>;

@end

<% if not user_overwritten? -%>
// Stub out '<%= overwrite_filename %>' as scaffolding and for testing.
// This will be removed if you add a <%= overwrite_filename %>.h file within 
// your project directory and re-run the generator.
@implementation <%= overwrite_filename %> @end
<% end -%>