#import <RestKit/RestKit.h>
#import <RestKit/CoreData.h>

@interface <%= filename %> : NSManagedObject

<% columns.each do |column| -%>
@property (nonatomic, retain) <%= ios_type(column.type) %><%= ios_attr_name(column.name) %>;
<% end -%>

<% associations.each do |association| -%>
@property (nonatomic, retain) <%= ios_association_type(association) %><%= ios_attr_name(association.name) %>;
<% end -%>

@end
